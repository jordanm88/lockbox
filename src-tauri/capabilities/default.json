{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Lockbox window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
